task projectinfo {
    doLast {
        println "Project Info"
        println "name: " + project.name + " version: " + project.version
        println "Gradle was found in " + gradle.gradleHomeDir
    }

}