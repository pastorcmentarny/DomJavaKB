package dms.pastor.examples.streams;

public class MathExamples {


/*
    public MonetaryAmount totalChargedAmount = products
            .stream()
            .map(OrderLine::getLineTotal)
            .reduce(FastMoney.of(0.00, "GBP"), MonetaryAmount::add);
 */
}
